// Styled Components
import { StyledLogo, StyledHeader } from './styles';

const Header = () => {

	return (
		<StyledHeader>
			<StyledLogo>CONSOLE.BLOG()</StyledLogo>
		</StyledHeader>
	);
};

export default Header;